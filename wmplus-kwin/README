wmplus-kwin
	wmplus for kwin (linux only)
	feature like wmplus for windows , but only feature 2 & 3 .
	and default hotkey change to win + f3 & win + f4 .
	use make to install . (plasma 5 only)
	use make kde4 to install . (plasma 4 only)
